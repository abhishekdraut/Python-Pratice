import json

# is used to dump or load the files ;
# dump -->converts the python datatype into json script
# load -->converts the Json string into datatype
# json.loads(x)
# json.dump(x)
x = int(input("Enter the age"))
y = str(x)
print(x)

print(type(x))
print(type(y))
